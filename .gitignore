# Dependencies
/node_modules
/bun.lockb
# If you use other lock files (less common with Bun, but safe to ignore)
/package-lock.json
/yarn.lock

# Build Output
# The default output directory for TypeScript compilation (e.g., when tsc is used)
/dist
/build
/out
# Common output for transpilers/bundlers
/lib

# Logs and Coverage
/logs
*.log
npm-debug.log*
yarn-debug.log*
# Test coverage reports
/coverage

# Environment Variables
# Crucial for security—do NOT commit these!
.env
.env.*
!.env.example # Keep example files tracked if needed

# Operating System & Editor Files
.DS_Store
Thumbs.db
# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json